version: 2.0
jobs:
 build:
  docker:
   - image: mcr.microsoft.com/dotnet/core/sdk:3.0-disco
  steps:
   - checkout
   - run:
      name: Restore packages
      command:
       dotnet restore
   - run:
      name: Build application
      command:
       dotnet build